{
  "enforce": {
    "gitPermissions": {
      "enabled": true,
      "description": "Block git add/commit/push based on checkboxes in CLAUDE.git.md"
    },
    "gitAddProtection": {
      "enabled": true,
      "description": "Block git add for files in .git/info/exclude"
    },
    "aiTraces": {
      "enabled": true,
      "description": "Remind about typography and AI-typical patterns before Write/Edit"
    },
    "languageRules": {
      "enabled": true,
      "description": "Remind about language consistency before Write/Edit"
    },
    "security": {
      "secrets": {
        "enabled": true,
        "description": "Block writing secrets (API keys, tokens, passwords) to files"
      },
      "dependencies": {
        "enabled": true,
        "warnOnly": true,
        "description": "Warn about package installations, suggest verification"
      }
    },
    "fileProtection": {
      "enabled": true,
      "description": "Block rm, del, git clean, and other destructive commands"
    },
    "promptInjection": {
      "enabled": true,
      "warnOnly": true,
      "description": "Warn about potential prompt injection in fetched content"
    }
  },
  "exclude": {
    "paths": [],
    "patterns": []
  },
  "defaultSource": "https://github.com/Marcel-Bich/marcel-bich-claude-dogma"
}
